# 发布准备度报告（release-readiness）

## 1) 已完成项
- 后台/API/shared/插件四层代码连通。
- 字段显示统一为 `中文名（key）`，并补缺省 tooltip 文案。
- 单位换算器增强（兼容 `=`、`≈`、`＝`、`约`）。
- 发布包结构与插件消费结构统一（含 `checksum`）。
- Seed 补齐 5 商品、2+ 替代关系、3 公式模板、1 发布版本、1 审计日志。
- 增加联调模拟页与文档。

## 2) 已验证项（静态）
- 配置文件完整性检查。
- 关键 import/export 路径检查。
- 前后端发布包字段一致性检查。
- 样例字段中文说明一致性复核。

## 3) 未验证项（运行态）
- `pnpm install` / `pnpm -r test` 未执行成功。
- API 实际启动与数据库 migration/seed 未实跑。
- 后台与插件真实浏览器联调未实跑。

## 4) 当前已知风险
1. 运行态依赖安装失败风险（网络代理）。
2. 未进行真实 tf.jdy.com 页面拦截验证。
3. Prisma schema 与 seed 在真实库环境可能出现约束冲突（需实跑确认）。
4. Admin 页面当前为轻量实现，缺少复杂校验与权限模型。

## 5) 上线前必须人工验证清单
- [ ] 后台页面中文显示检查（字段、tooltip、缺省文案）
- [ ] 公式模板实际计算检查（含 fallback）
- [ ] 单位换算检查（`=`、`≈`、异常公式）
- [ ] 插件拦截接口检查（仅两类目标接口）
- [ ] 规则发布/回滚检查（latest/history 一致）
- [ ] 缓存回退检查（断网时使用最近成功缓存）
- [ ] 联调模拟页结果与插件计算结果一致性核对
